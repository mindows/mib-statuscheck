## What and why

<!-- What does this change, and why? Link the issue: "Fixes #12". -->

## How I checked it

<!-- e.g. restarted the shell, added/removed a service, triggered a notification. -->

- [ ] `omarchy plugin validate .` passes
- [ ] Tried it in a running shell (`omarchy restart shell`)
- [ ] README updated, if settings, keys, IPC commands or presets changed
- [ ] New presets: the `summary.json` check output is included above
